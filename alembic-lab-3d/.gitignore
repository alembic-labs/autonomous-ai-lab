node_modules
dist
.DS_Store

# Original (uncompressed) GLB exports from Meshy / Sketchfab. Large
# binaries we don't want in git. Source assets stay local; the
# committed runtime assets live in public/3d-lab/ and are produced by
# `npm run compress-assets`. Re-export from Meshy if needed.
public/3d-lab-source/
